
INCFLAGS = -I$(shell root-config --incdir) -I/usr/local/pgsql/include
LDFLAGS = $(shell root-config --libs) -L/usr/local/pgsql/lib -lpq

#CPPFLAGS = -I/usr/local/pgsql/include
#CFLAGS   += -g
#LDFLAGS  += -g
#LDLIBS   = -L/usr/local/pgsql/lib -lpq

#INCFLAGS = -I/usr/local/pgsql/include
#LDFLAGS = -L/usr/local/pgsql/lib -lpq
BOOST_ROOT = /usr/local/boost_1_59_0
BOOST_LIBDIR = $(BOOST_ROOT)/stage/lib
BOOST_LIBS = -lboost_program_options -lboost_regex
INCFLAGS += -I$(BOOST_ROOT)
LDFLAGS += -L$(BOOST_LIBDIR) -Wl,-rpath,$(BOOST_LIBDIR) $(BOOST_LIBS)

make : 
	g++ -std=c++11 -Wall $(INCFLAGS) $(LDFLAGS) ColumnConfigParser.cc TupleReader.cc PostgresConnector.cc example_usage.cc -o test

clean:
	rm test
